# 👤 Client Service

Сервис управления пользователями и контроля доступа.

## Описание

Client Service управляет пользовательскими аккаунтами, ролями и правами доступа в системе. Обеспечивает централизованное управление пользователями и их разрешениями.

## Основные возможности

- **CRUD операции с пользователями** - Полный жизненный цикл управления пользователями
- **Система ролей и permissions** - Гибкая система прав доступа
- **Блокировка/разблокировка пользователей** - Управление доступом к системе
- **Валидация прав доступа** - Проверка разрешений для других сервисов
- **Интеграция с Auth** - Синхронизация с сервисом аутентификации
- **PostgreSQL** - Надежное хранение пользовательских данных

## Технологии

- **Go** 1.23.1
- **PostgreSQL** - основное хранилище данных
- **HTTP/REST API** - основной интерфейс
- **JWT** - валидация токенов

## Структура проекта

```
client/
├── app/
│   ├── cmd/           # Точка входа приложения
│   ├── internal/      # Внутренняя логика
│   │   ├── api/       # HTTP handlers и роутинг
│   │   ├── clients/   # HTTP клиенты (Auth)
│   │   ├── entity/    # Бизнес-сущности
│   │   ├── repository/# Работа с БД
│   │   └── service/   # Бизнес-логика
│   ├── migrations/    # SQL миграции
│   └── pkg/          # Переиспользуемые пакеты
└── docker/           # Docker конфигурация
```

## Запуск

### Локальная разработка

1. Настройте переменные окружения:
```bash
cp example.env .env
# Отредактируйте .env файл
```

2. Запустите зависимости (PostgreSQL):
```bash
cd docker
docker-compose up -d
```

3. Примените миграции:
```bash
cd app
make migrate-up
```

4. Запустите сервис:
```bash
make run
```

### Docker

```bash
cd docker
docker-compose -f docker-compose.yml -f docker-compose.override.yml up
```

## API Документация

После запуска Swagger документация доступна по адресу:
```
http://localhost:8081/swagger/index.html
```

## Конфигурация

Основные переменные окружения:

```env
# Server
SERVER_PORT=8081
LOG_LEVEL=debug

# PostgreSQL
POSTGRES_DSN=postgresql://user:password@localhost:5432/client_db
POSTGRES_MAX_CONNS=10

# Auth Service
AUTH_SERVICE_URL=http://localhost:8080
```

## Модель данных

### Роли
- **Admin** - Полный доступ к системе
- **Manager** - Управление пользователями
- **User** - Базовый доступ

### Permissions
Гранулярная система прав для тонкой настройки доступа к ресурсам.

## Разработка

### Тестирование

```bash
make test
```

### Линтинг

```bash
make lint
```

### Генерация Swagger документации

```bash
make swagger
```

## Зависимости

- [github.com/samandr77/microservices/auth](../auth) - для валидации токенов

## Ссылки

- [Репозиторий](https://github.com/samandr77/microservices)
- [Документация по настройке](docs/00-enviroments.md)
- [Разработка](docs/01-dev-run.md)
- [Production](docs/02-production-run.md)
