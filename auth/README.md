# 🔐 Auth Service

Сервис аутентификации и авторизации для микросервисной архитектуры.

## Описание

Auth Service отвечает за безопасную аутентификацию пользователей и управление сессиями в системе. Сервис интегрирован со Сбер ID и обеспечивает защиту от атак методом перебора.

## Основные возможности

- **JWT токены** - Генерация и валидация JWT токенов для авторизации
- **Коды верификации** - Отправка и проверка кодов для входа
- **Интеграция со Сбер ID** - OAuth авторизация через Сбер ID
- **Защита от брутфорса** - Отслеживание попыток входа (attempt tracking)
- **Асинхронные уведомления** - Отправка событий через Kafka
- **Хранение сессий** - PostgreSQL для надежного хранения данных

## Технологии

- **Go** 1.23.1
- **PostgreSQL** - основное хранилище данных
- **Kafka** - асинхронная коммуникация с другими сервисами
- **JWT** - токены авторизации
- **HTTP/REST API** - основной интерфейс

## Структура проекта

```
auth/
├── app/
│   ├── cmd/           # Точка входа приложения
│   ├── internal/      # Внутренняя логика
│   │   ├── api/       # HTTP handlers и роутинг
│   │   ├── clients/   # HTTP клиенты (Sber ID, Users)
│   │   ├── entity/    # Бизнес-сущности
│   │   ├── repository/# Работа с БД
│   │   └── service/   # Бизнес-логика
│   ├── migrations/    # SQL миграции
│   └── pkg/          # Переиспользуемые пакеты
└── docker/           # Docker конфигурация
```

## Запуск

### Локальная разработка

1. Настройте переменные окружения:
```bash
cp example.env .env
# Отредактируйте .env файл
```

2. Запустите зависимости (PostgreSQL, Kafka):
```bash
cd docker
docker-compose up -d
```

3. Примените миграции:
```bash
cd app
make migrate-up
```

4. Запустите сервис:
```bash
make run
```

### Docker

```bash
cd docker
docker-compose -f docker-compose.yml -f docker-compose.override.yml up
```

## API Документация

После запуска Swagger документация доступна по адресу:
```
http://localhost:8080/swagger/index.html
```

## Конфигурация

Основные переменные окружения:

```env
# Server
SERVER_PORT=8080
LOG_LEVEL=debug

# PostgreSQL
POSTGRES_DSN=postgresql://user:password@localhost:5432/auth_db
POSTGRES_MAX_CONNS=10

# Kafka
KAFKA_BROKERS=localhost:9092
KAFKA_NOTIFICATION_TOPIC=notifications

# Sber ID
SBER_ID_CLIENT_ID=your_client_id
SBER_ID_CLIENT_SECRET=your_client_secret
SBER_ID_REDIRECT_URL=http://localhost:8080/auth/sber/callback

# JWT
JWT_SECRET=your_secret_key
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
```

## Разработка

### Тестирование

```bash
make test
```

### Линтинг

```bash
make lint
```

### Генерация Swagger документации

```bash
make swagger
```

## Зависимости

- [github.com/samandr77/microservices/client](../client) - для проверки пользователей

## Ссылки

- [Репозиторий](https://github.com/samandr77/microservices)
- [Документация по настройке](docs/00-enviroments.md)
- [Разработка](docs/01-dev-run.md)
- [Production](docs/02-production-run.md)
